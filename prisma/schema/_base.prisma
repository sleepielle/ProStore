generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // If you also use a direct connection for certain CLI operations, add:
  // directUrl = env("DIRECT_DATABASE_URL")
}
