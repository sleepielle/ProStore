generator client {
  provider = "prisma-client-js"
  output   = "../../src/generated/prisma" // from prisma/schema/schema.prisma to /src/generated/prisma
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
